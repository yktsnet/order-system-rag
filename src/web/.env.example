# フロントエンドが接続する API のベース URL。
# 未設定なら localhost のデフォルトにフォールバックする（src 内の ?? を参照）。

# RAG API（本リポの src/api/main.py。DocumentsTab / SearchTab が使用）
VITE_RAG_API_BASE=http://localhost:8002

# Text-to-SQL API（別リポ order-system-migration の Agent サービス。SearchTab が使用）
VITE_SQL_API_BASE=http://localhost:5153
